{
  "name": "teatro-prenotazioni",
  "private": true,
  "description": "Prenotazione posti teatro - avvio servizi in locale",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"python backend/run.py\" \"npm run dev --prefix frontend\"",
    "dev:backend": "python backend/run.py",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
